plugins {
    id 'java'
    id 'application'
}

group = 'org.neatore.calisync'

application {
    mainClass = "org.neatore.calisync.CaliSync"
}

dependencies {
    implementation 'org.java-websocket:Java-WebSocket:1.6.0'
}
